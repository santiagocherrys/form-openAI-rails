<div  >
    <p class="mb-10" style="color: green"><%= notice %></p>


    <!-- recordar que si uso el primero quito el # y pongo = -->
    <%# render partial: 'form', locals: { form: @form } %>

    <%= render @form%>
    <%= link_to "back to index" , forms_path, class: "flex-1 ml-3 bg-white py-2 px-5 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 hover:bg-gray 50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500"%>
<div>

